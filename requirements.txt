# Core dependencies
langchain>=0.1.0
langchain-community>=0.0.10
faiss-cpu>=1.7.4
sentence-transformers>=2.2.0
PyPDF2>=3.0.0
rouge-score>=0.1.2
pyyaml>=6.0
numpy>=1.24.0

# LLM inference (GPU)
torch>=2.0.0
transformers>=4.35.0

# 4-bit quantization (GPU only, optional on CPU)
bitsandbytes>=0.41.0
